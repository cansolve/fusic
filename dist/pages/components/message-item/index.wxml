<block>
    <view>
        <view class="list">
            <view class="list-main">
                <view class="list-main-left">
                    <at-avatar circle="{{true}}" __triggerObserer="{{ _triggerObserer }}" image="{{image}}"></at-avatar>
                </view>
                <view class="list-main-right">
                    <view class="margin-bottom-15"><text>{{title}}</text>
                    </view>
                    <view class="margin-bottom-15"><text class="subTitle">{{subTitle}}</text>
                    </view>
                </view>
            </view>
        </view>
    </view>
</block>