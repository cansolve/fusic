<block>
    <view class="page">
        <view class="page-top"></view>
        <view class="page-main">
            <movie-showing-item data="{{movieData}}" __triggerObserer="{{ _triggerObserer }}"></movie-showing-item>
        </view>
        <view class="page-bottom">
            <bottom-tab tab="{{currentTab}}" __triggerObserer="{{ _triggerObserer }}"></bottom-tab>
        </view>
    </view>
</block>